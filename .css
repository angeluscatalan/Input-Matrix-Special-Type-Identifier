body {
    font-family: Arial, sans-serif;
    margin: 0;
    height: 100vh;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
}

.input {
    position: absolute;
    top: 20px;
    width: 100%;
    text-align: center;
}

.matrix {
    margin-top: 20px;
}

table {
    border-collapse: collapse;
}

td {
    padding: 10px;
    text-align: center;
}

.special-matrix {
    display: none;
    margin-top: 20px;
}